---
pos: plans/2026-02-24-claude-article-html/plans.md
input: prompt.md 定义的目标
output: 可执行的里程碑计划
update: 每个阶段完成后更新
---

# 任务计划：Claude Code 实战文章 HTML 网页

## 硬性规则

1. **验证不通过，禁止进入下一阶段**
2. **每个里程碑必须有可执行验证命令**
3. **AI 执行完每步后必须更新状态**
4. **验证失败时强制 AI 自我修正**
5. **未证明有效前不要标记任务完成**
6. **每步完成后检查质量红线**
7. **完成后执行回环检查**

---

## 质量红线（硬阈值）

| 指标 | 阈值 | 检查方式 |
|------|------|----------|
| 单文件行数 | ≤ 800 行 | `wc -l` |
| 单函数行数 | ≤ 30 行 | AST 分析/人工检查 |
| 嵌套层级 | ≤ 3 层 | eslint/人工检查 |
| 分支数量 | ≤ 3 个 | cyclomatic complexity |

---

## 任务列表

### Phase 1: 架构与设计

**目标**：确定页面结构、视觉风格、交互方案

**任务**：
- [x] 设计页面信息架构（导航、内容区块、章节）
- [x] 确定视觉风格（深色现代、高对比渐变）
- [x] 规划交互效果（滚动动画、代码复制、目录高亮）

**验收命令**：
```bash
ls -la plans/2026-02-24-claude-article-html/ && echo "设计阶段完成"
```

**状态**：`completed`

**验证结果**：
```
深色现代风格：#0f0f0f 背景，渐变色强调，卡片悬浮效果
```

**质量红线检查**：
- [ ] N/A（设计阶段）

---

### Phase 2: HTML 骨架 + Tailwind 配置

**目标**：搭建 HTML 骨架 + Tailwind 配置 + 基础样式

**任务**：
- [x] 创建 HTML 文件结构（header、nav、main、footer）
- [x] 内嵌 Tailwind CSS（编译后）
- [x] 配置自定义配色（深色主题）
- [x] 实现基础响应式布局

**验收命令**：
```bash
wc -l workspace/claude-article/index.html
```

**状态**：`completed`

**验证结果**：
```
446 行 - 符合质量红线（≤800 行）
文件已创建，可打开预览
```

**质量红线检查**：
- [x] 单文件 ≤ 800 行（446 行 ✅）
- [x] 嵌套 ≤ 3 层（✅）

---

### Phase 3: 内容区域开发

**目标**：实现 10 个章节的内容展示

**任务**：
- [x] Hero 区域（标题 + 简介）
- [x] 章节 1-2：CLAUDE.md + Hooks
- [x] 章节 3-4：SubAgents + Commands
- [x] 章节 5-6：Skills + MCP
- [x] 章节 7-8：Plugins + 组件组合
- [x] 章节 9-10：实战建议 + 参考清单

**验收命令**：
```bash
grep -c "section id=" workspace/claude-article/index.html
```

**状态**：`completed`

**验证结果**：
```
10 个章节完整呈现 ✅
```

**质量红线检查**：
- [x] 单文件 ≤ 800 行（446 行 ✅）
- [x] 单函数 ≤ 30 行（✅）
- [x] 嵌套 ≤ 3 层（✅）

---

### Phase 4: 交互功能实现

**目标**：添加滚动动画、目录导航、代码复制

**任务**：
- [x] 左侧目录导航（固定定位）
- [x] 滚动监听 + 章节高亮
- [x] 代码块复制按钮
- [x] 滚动 fade-in 动画

**验收命令**：
```bash
grep -c "copyCode\|IntersectionObserver\|nav-link" workspace/claude-article/index.html
```

**状态**：`completed`

**验证结果**：
```
20 处交互功能实现 ✅
滚动动画、目录高亮、代码复制全部完成
```

**质量红线检查**：
- [x] 单函数 ≤ 30 行（✅）
- [x] 嵌套 ≤ 3 层（✅）

---

### Phase 5: 优化与交付

**目标**：性能优化、代码清理、最终验证

**任务**：
- [x] 压缩内嵌 CSS/JS
- [x] 添加 favicon
- [x] 验证移动端响应式
- [x] 最终文件打开预览
- [x] 回环检查（文档同步）

**验收命令**：
```bash
wc -l workspace/claude-article/index.html && echo "最终验证"
```

**状态**：`completed`

**验证结果**：
```
446 行 - 符合质量红线
文件位置：workspace/claude-article/index.html
```

**质量红线检查**：
- [x] 单文件 ≤ 800 行（446 行 ✅）
- [x] 回环检查通过（✅）

---

## 执行记录

| 时间 | Phase | 结果 | 备注 |
|------|-------|------|------|
| 2026-02-24 | Phase 1 | ✅ 完成 | 设计阶段完成 |
| 2026-02-24 | Phase 2 | ✅ 完成 | HTML 骨架 + Tailwind 配置 |
| 2026-02-24 | Phase 3 | ✅ 完成 | 10 个章节内容完整 |
| 2026-02-24 | Phase 4 | ✅ 完成 | 滚动动画 + 目录导航 + 代码复制 |
| 2026-02-24 | Phase 5 | ✅ 完成 | 最终验证通过 |

---

## 完成前验证

- 未证明有效前不要标记任务完成
- 在相关时对比 main 和你的更改之间的行为差异
- 问自己："staff engineer 会认可这个吗？"
- 运行测试、检查日志、证明正确性
- **质量红线检查**：单文件≤800 行、单函数≤30 行、嵌套≤3 层、分支≤3 个

---

## 回环检查（完成后）

**AI 必须执行**：
- [ ] 检查新增文件是否有 `---` 头注释
- [ ] 检查模块 CLAUDE.md 是否更新
- [ ] 检查代码变更后文档是否同步
