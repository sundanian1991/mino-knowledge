# Plans - 任务拆解与验证

> **状态**：待用户确认
>
> 核心机制：小里程碑 + 强制验证
> 每个任务控制在 1-2 小时可完成

---

## AI 必读

**编辑完此文件后必须**：
1. 向用户展示 `prompt.md` 和 `plans.md` 草案
2. 询问"任务拆解是否合理？有无需要调整优先级或补充的？"
3. **等待用户说"开始执行"或确认后才能执行**

---

## 规则

1. **验证不通过，禁止进入下一阶段**
2. **每个里程碑必须有可执行验证命令**
3. **AI 执行完每步后必须更新状态**

---

## 任务列表

### Phase 1: 架构与设计

**目标**：确定页面结构、视觉风格、交互方案

**任务**：
- [x] 设计页面信息架构（导航、内容区块、学习路径）
- [x] 确定视觉风格（深色现代、高对比渐变）
- [x] 规划交互效果（滚动动画、卡片展开、代码高亮）

**验收命令**：
```bash
ls -la plans/2026-02-24-claude-guide/ && echo "设计阶段完成"
```

**状态**：`completed`

**验证结果**：
```
设计阶段完成
采用深色现代风格：#0f0f0f 背景，渐变色强调，卡片悬浮效果
```

---

### Phase 2: 核心框架开发

**目标**：搭建 HTML 骨架 + Tailwind 配置 + 基础样式

**任务**：
- [x] 创建 HTML 文件结构
- [x] 配置 Tailwind CDN + 自定义配色
- [x] 实现响应式导航栏
- [x] 实现 Hero 区域（标题 + 简介）

**验收命令**：
```bash
open workspace/claude-guide/index.html
```

**状态**：`completed`

**验证结果**：
```
HTML 结构完整，Tailwind 配置正确，响应式导航正常工作
```

---

### Phase 3: 七大组件卡片

**目标**：实现七大组件的可视化展示卡片

**任务**：
- [ ] CLAUDE.md 卡片（项目记忆）
- [ ] Hooks 卡片（自动任务）
- [ ] SubAgents 卡片（任务分解）
- [ ] Commands 卡片（斜杠命令）
- [ ] Skills 卡片（可复用技能）
- [ ] MCP Servers 卡片（外部连接）
- [ ] Plugins 卡片（打包工作流）

**验收命令**：
```bash
# 检查所有卡片是否渲染正常
open workspace/claude-guide/index.html
```

**状态**：`pending`

**验证结果**：
```
待验证
```

---

### Phase 4: 交互功能实现

**目标**：添加滚动动画、卡片交互、代码预览

**任务**：
- [ ] 滚动触发 fade-in 动画
- [ ] 卡片 hover 效果（抬升 + 阴影）
- [ ] 代码块语法高亮 + 复制按钮
- [ ] 组件组合案例展示（手风琴/标签页）

**验收命令**：
```bash
# 浏览器验证交互效果
open workspace/claude-guide/index.html
```

**状态**：`pending`

**验证结果**：
```
待验证
```

---

### Phase 5: 优化与交付

**目标**：性能优化、代码清理、最终验证

**任务**：
- [ ] 压缩内嵌 CSS/JS
- [ ] 添加 favicon
- [ ] 验证移动端响应式
- [ ] 最终文件打开预览

**验收命令**：
```bash
# 最终验证
open workspace/claude-guide/index.html
```

**状态**：`pending`

**验证结果**：
```
待验证
```

---

## 执行记录

| 时间 | Phase | 结果 | 备注 |
|------|-------|------|------|
| | | | |
